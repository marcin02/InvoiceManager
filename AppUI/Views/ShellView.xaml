<Window x:Class="AppUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppUI.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="1000" FontFamily="Calibri">
    <Window.Resources>
        <Style x:Key="TextBlockTrigger" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=Type ,Path=Text}" Value="Wpłata">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="Margin" Value="4,0,0,0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ExpandTrigger" TargetType="DockPanel">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=ExpandBtn, Path=IsChecked}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Border Padding="10" Background="DodgerBlue" Grid.Row="0" Grid.RowSpan="3">
        <StackPanel Background="DodgerBlue" >
            <Button Content="Dodaj wpis"/>
            <TextBlock Text="Filtry" HorizontalAlignment="Center" FontSize="16" Margin="0,0,0,10"/>
            <TextBlock Text="Rodzaj transakcji"/>
            <ComboBox x:Name="TransactionType" SelectedIndex="0"/>
            <TextBlock Text="Użytkownicy"/>
            <ComboBox ItemsSource="{Binding Users}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding FullName}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Text="Data"/>
            <DockPanel>
                <TextBlock Text="Od"/>
                <DatePicker/>
                <TextBlock Text="Do"/>
                <DatePicker/>
            </DockPanel>
        </StackPanel>
        </Border>
        <DockPanel Grid.Column="1" Grid.Row="0" Background="RoyalBlue">
            <TextBox Margin="10"/>
        </DockPanel>
        <Border Grid.Column="1" Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,0,0,1">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="65"/>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" Text="Data"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Margin="10,0,0,0" Text="Tytuł"/>
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Kwota"/>
                <TextBlock Grid.Row="0" Grid.Column="4" Text="Saldo"/>
                <TextBlock Grid.Row="0" Grid.Column="5" Text="Typ transakcji"/>
                <TextBlock Grid.Row="0" Grid.Column="6" Margin="10,0,0,0" Text="Użytkownik"/>
            </Grid>
        </Border>
        <ScrollViewer Grid.Column="1" Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TransactionFull">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="0,0,0,1">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="350"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <ToggleButton x:Name="ExpandBtn" Grid.Column="0"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" Text="{Binding Date}"/>
                                <TextBlock Grid.Row="0" Grid.Column="2" Style="{StaticResource TextBlockTrigger}" Margin="10,0,0,0" Text="{Binding Title}"/>
                                <TextBlock Grid.Row="0" Grid.Column="3" Style="{StaticResource TextBlockTrigger}" Text="{Binding Amount}"/>
                                <TextBlock Grid.Row="0" Grid.Column="4" Text="{Binding Balance}"/>
                                <TextBlock Grid.Row="0" Grid.Column="5" x:Name="Type" Text="{Binding TransactionType.Type}"/>
                                <TextBlock Grid.Row="0" Grid.Column="6" Text="{Binding Users.FullName}"/>
                                <DockPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="4" Margin="5" Style="{StaticResource ExpandTrigger}" LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" Text="{Binding TransactionInfo.Company}"/>
                                    <TextBlock Margin="250,0,0,0" Text="{Binding TransactionInfo.Invoice}"/>
                                </DockPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
